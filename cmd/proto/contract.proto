syntax = "proto3";

package contracts;
option go_package = "contracts";

enum Gender {
    UNKNOWN = 0;
    MALE = 1;
    FEMALE = 2;
    UNDISCLOSED = 3;
}

message Person {
    string id = 1;
    string name = 2;
    int32 age = 3;
    Gender gender = 4;
}